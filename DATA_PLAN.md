# RainGuard 데이터 계획

## 현재 데이터

`data/sample_risk_points.csv`는 UI와 지도 표시를 검증하기 위한 시연용 샘플 데이터입니다.

## 실제 데이터로 교체할 항목

| 데이터 | 사용 목적 |
| --- | --- |
| 침수흔적도 | 과거 침수 발생 지역 라벨 생성 |
| 강수량/강수 예보 | 시나리오 또는 실시간 위험도 입력값 |
| 도로망 데이터 | 최단경로와 안전경로 계산 |
| 행정구역 경계 | 분석 지역 선택 및 지도 필터링 |

## 모델 고도화 방향

1. 분석 지역을 격자로 나눈다.
2. 각 격자에 침수 이력, 강수량, 침수 지역과의 거리, 도로 밀도 feature를 붙인다.
3. 침수 여부를 label로 만들어 `flood_dataset.csv`를 생성한다.
4. RandomForest 모델로 침수 위험 확률을 예측한다.
5. 도로 구간 비용에 위험도를 반영해 안전경로를 계산한다.
